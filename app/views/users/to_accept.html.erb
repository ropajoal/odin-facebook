<ul>
  <% @users.each do |user| %>
    <li><%= link_to user.username, user_show_path(user.username) %> <%= render partial: "frienship_requester", locals: {friendship: @friendships.find{|f| f.user1_id == user.id or f.user2_id == user.id}, user: user } %> </li>
  <% end %>
</ul>
